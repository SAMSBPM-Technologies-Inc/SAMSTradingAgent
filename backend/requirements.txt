# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# MongoDB async driver
motor==3.4.0
pymongo==4.7.2

# Data & ML
yfinance==0.2.40
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2
xgboost==2.0.3
ta==0.11.0          # Technical analysis library (RSI, MACD, etc.)

# Scheduling
apscheduler==3.10.4

# Config & validation
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# HTTP client
httpx==0.27.0

# Phase 1: real data sources
fredapi==0.5.1          # FRED macro data (rates, CPI, unemployment, VIX)
vaderSentiment==3.3.2   # VADER sentiment analysis on news headlines (no API key needed)

# Phase 3: AI analyst
# Floor set by what the analyst actually calls: adaptive thinking and
# output_config.effort. An older SDK silently lacks output_config, so the
# previous >=0.30.0 would let a rebuild install a version that cannot run
# the analyst at all.
anthropic>=0.122.0      # Claude API for AI analyst layer

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
email-validator==2.1.1
cryptography==42.0.8

# Logging
structlog==24.1.0

# Utilities
python-dateutil==2.9.0

# Automated trading — Interactive Brokers API wrapper.
# ib_async is the maintained successor to ib_insync (unmaintained since 2024).
# Drop-in API-compatible; supports Python 3.10–3.14.
ib_async==2.1.0
